@model DetailsViewModel<BaseClass>

<partial name="_TitleBar" model="@{new TitleBarViewModel{ActionType = ActionTypes.Details, Title = "سایت " + Model.Row.Title};}" />

<partial name="Details_partial" model="Model" />

<partial name="_TitleBar" model="@{new TitleBarViewModel{ActionType = ActionTypes.Index, Title = $"فهرست {repository.Translate(Terms.PluralCategory)} این سایت", AddNewController = "Category", AddNewTitle = repository.Translate(Terms.AddNewCategory), ParentID = Model.Row.ID};}" />

<partial name="_Grid" model="ViewBag.Categories" />
