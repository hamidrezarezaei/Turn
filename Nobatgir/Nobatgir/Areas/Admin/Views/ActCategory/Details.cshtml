@model DetailsViewModel<BaseClass>

<partial name="_TitleBar" model="@{new TitleBarViewModel{ActionType = ActionTypes.Details, Title = "دسته منوی " + Model.Row.Title};}" />

<partial name="Details_partial" model="Model" />

<partial name="_TitleBar" model="@{new TitleBarViewModel{ActionType = ActionTypes.Index, Title = "لیست منوهای این دسته" , AddNewController = "Act", AddNewTitle = "ایحاد منوی جدید", ParentID = Model.Row.ID};}" />

<partial name="_Grid" model="ViewBag.Acts"/>
